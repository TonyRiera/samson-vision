# Terminal / error example

**Input:** `blueprint_weak_points.png` — structural blueprint with annotated weak points.

**Output:** `output.svp.md` — spatial zones suitable for error-location reasoning.

Use for CLI output, log panes, stack traces, and annotated failure screenshots.
