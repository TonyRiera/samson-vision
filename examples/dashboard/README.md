# Dashboard example

**Input:** `pillars_as_data.png` — data-dense visual (pillars as metrics metaphor).

**Output:** `output.svp.md` — density map, color palette, region hierarchy.

Use for KPI panels, analytics screenshots, and chart-as-image QA.
